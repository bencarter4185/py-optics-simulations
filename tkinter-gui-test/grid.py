from tkinter import *

root = Tk()

# Creating a label widget
myLabel = Label(root, text = "Hello world!")
# Shove label onto the screen
myLabel.pack()

# Run the 'main loop' to open application
root.mainloop()